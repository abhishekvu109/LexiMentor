#Apply the environment dependency
apiVersion: v1
kind: ConfigMap # Resource type
metadata:
  name: auth-config
  labels:
    app: auth-app
data:
  SERVICE_PORT: "4848"
  MYSQL_HOST: "mysql-service-k8s.default.svc.cluster.local"
  MYSQL_PORT: "3306"
  SERVICE_NAME: "auth-service-k8s.default.svc.cluster.local"
  JWT_SECRET: "W09fL1VDmiTpolPS1M4yd/Ipnyu/ZQLDEA875WQWMfM="

---

# Apply the deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-deployment-k8s
spec:
  selector:
    matchLabels:
      app: auth-app
  replicas: 1
  template:
    metadata:
      labels:
        app: auth-app
    spec:
      containers:
        - name: auth-app
          image: localhost:5000/auth-service:latest
          env:
            - name: SERVICE_PORT
              valueFrom:
                configMapKeyRef:
                  name: auth-config
                  key: SERVICE_PORT
            - name: MYSQL_HOST
              valueFrom:
                configMapKeyRef:
                  name: auth-config
                  key: MYSQL_HOST
            - name: MYSQL_PORT
              valueFrom:
                configMapKeyRef:
                  key: MYSQL_PORT
                  name: auth-config
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  key: username
                  name: mysql-user-secret
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: password
                  name: mysql-user-secret
            - name: SERVICE_NAME
              valueFrom:
                configMapKeyRef:
                  key: SERVICE_NAME
                  name: auth-config
          imagePullPolicy: Always
          ports:
            - containerPort: 4848
#      imagePullSecrets:
#        - name: my-registry-secret

---

# Adding the service

apiVersion: v1
kind: Service
metadata:
  name: auth-service-k8s
spec:
  selector:
    app: auth-app
  ports:
    - port: 4848
      targetPort: 4848
  type: ClusterIP